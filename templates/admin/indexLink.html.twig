{% extends 'base.html.twig' %}

{% block title %}Hello Admin!
{% endblock %}

{% block body %}

<div class="w-50 ms-auto me-auto mt-4">
    <h1>Hello {{ user.username }} !</h1>
     <div class="d-flex justify-content-between align-items-center mb-3">
                <div class="d-flex justify-content-between w-100">
                    <a href="{{ path('admin_index') }}" class="btn btn-add">Switch to Users <i class="bi bi-person-plus ms-2"></i> </a>
                    
                </div>
            </div>
    {% include 'link/index.html.twig' %}
</div>
{% endblock %}
