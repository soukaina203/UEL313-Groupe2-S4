{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1 class="mt-3 text-center">Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex mt-3 ms-auto me-auto justify-content-center" style="width:fit-content;">
         <a class="btn btn-add d-flex ms-auto me-auto" href="{{ path('admin_index') }}" style="width:fit-content;"><i class="bi bi-arrow-return-left me-2"></i>back to list</a>
         {{ include('user/_delete_form.html.twig') }}
    </div>


    
{% endblock %}
